// The tickets layout (./layout.tsx) renders the views sidebar and the
// active view's ticket list. There's nothing route-specific to render
// at /tickets itself — the layout shows the full list when no detail is
// open, and overlays the compact queue + detail when on /tickets/[id].
export default function TicketsPage() {
  return null
}
